# Bun Configuration
# https://bun.sh/docs/runtime/bunfig

[test]
# Preload test isolation script to ensure tests don't affect real user data
# This sets ULTRAWORK_TEST_BASE_DIR before any test files are loaded
preload = ["./tests/ultrawork/preload.js"]

# Test coverage settings (optional)
# coverage = true
# coverageDir = "coverage"
